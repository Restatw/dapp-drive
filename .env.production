# Production (deployed to IPFS) — call the local IPFS node directly.
# No Vite proxy available, so the user's browser must reach these endpoints.
VITE_IPFS_API=http://127.0.0.1:5001
VITE_IPFS_GW=http://127.0.0.1:8080
